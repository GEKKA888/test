<div class="main users-edit">
  <div class="container">
    <h2 class="form-heading">アカウント編集</h2>
    <div class="form users-form">
      <div class="form-body">
        <% @user.errors.full_messages.each do |message| %>
          <div class="form-error">
            <%= message %>
          </div>
        <% end %>
        <%= form_for(@user, url: user_path(@user), html: {method: "patch"}) do |f| %>
        <%= render partial: 'form' , locals: {f: f} %><br>
        <%= submit_tag("保存", class: 'btn btn-info btn-new link-color') %>
        <% end %>
      </div>
    </div>
  </div>
</div>
